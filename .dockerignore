node_modules
.next
.git
*.db
*.db-journal
prisma/dev.db*
.env
.env.local
npm-debug.log*
# 本地 Docker 绑定挂载的数据目录（postgres 容器写入，宿主无读权限会阻断构建上下文）
data/
docs
README.md
# 注意：不能忽略 scripts —— build 脚本会调 tsx scripts/pack-extension.ts 打包插件 zip。
# 只忽略明确不需要进构建上下文的脚本（shell 运维脚本）。
scripts/*.sh
# 本地 Docker 入口必须进构建上下文（上面的 scripts/*.sh 会把它挡掉）
!scripts/docker-local-entrypoint.sh
!scripts/docker-private-db-init.sh
.next-verify
# 整机版分发包：是构建产物的副本，带完整 ts 源码，会与主工程类型冲突
public/downloads/beacon-appliance-*
# 桌面壳（Tauri）：纯客户端子项目，服务器构建用不到；src-tauri/target 近 1GB
desktop

# 签名凭据：**绝不进构建上下文**。Dockerfile 是 `COPY . .`，不排的话
# deploy/private/signing/ 里的 Developer ID .p12 会被烤进镜像层，
# 而镜像层删不掉——同一台机上另一个项目的容器里就躺着一份两个月前烤进去的 Apple AuthKey。
# rsync 那边也要 --exclude 'deploy/private'（部署闸门 2/4 会拦，2026-08-31 真拦下过一次）。
deploy/private
